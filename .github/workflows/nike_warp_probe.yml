name: Nike WARP Probe (temp)

on:
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Instalar curl_cffi
        run: pip install curl_cffi

      - name: Instalar Cloudflare WARP
        run: |
          curl -fsSL https://pkg.cloudflareclient.com/pubkey.gpg | sudo gpg --yes --dearmor -o /usr/share/keyrings/cloudflare-warp-archive-keyring.gpg
          echo "deb [arch=amd64 signed-by=/usr/share/keyrings/cloudflare-warp-archive-keyring.gpg] https://pkg.cloudflareclient.com/ $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/cloudflare-client.list
          sudo apt-get update
          sudo apt-get install -y cloudflare-warp

      - name: Conectar WARP em modo proxy (SOCKS5 :40000)
        run: |
          sudo systemctl start warp-svc || (sudo warp-svc >/tmp/warpsvc.log 2>&1 &)
          sleep 6
          warp-cli --accept-tos registration new || warp-cli --accept-tos register || true
          warp-cli --accept-tos mode proxy || warp-cli --accept-tos set-mode proxy || true
          warp-cli --accept-tos connect || true
          sleep 6
          warp-cli --accept-tos status || true
          echo "--- trace direto ---"
          curl -s https://www.cloudflare.com/cdn-cgi/trace | grep -E 'ip=|warp=' || true
          echo "--- trace via WARP socks5 ---"
          curl -s --socks5-hostname 127.0.0.1:40000 https://www.cloudflare.com/cdn-cgi/trace | grep -E 'ip=|warp=' || true

      - name: Sonda Nike via WARP
        env:
          NIKE_PROXY: socks5h://127.0.0.1:40000
        run: python nike_warp_probe.py
